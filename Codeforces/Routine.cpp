#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <string>
using namespace std;

int main(){
    int a, b, c, d;
    cin >> a >> b >> c >> d;

    int n = min(a,b);
    int area = c * d;
    







}

