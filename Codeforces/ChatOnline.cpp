#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
#include <set>
#include <unordered_map>
using namespace std;

int main(){
    int p, q, l, r;
    cin >> p >> q >> l >> r;

    int z[p][2];
    int x[q][2];

    for(int i = 0; i < p; i++){
        int a, b;
        cin >> a >> b;
        a = z[p][0];
        b = z[p][1];
    }

    for(int i = 0; i < q; i++){
        int c, d;
        cin >> c >> d;
        c = z[q][0];
        d = z[q][1];
    }



}