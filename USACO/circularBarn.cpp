#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main(){
    // reopen("cbarn.in", "r", stdin);
    // freopen("cbarn.out", "w", stdout);

    //inputs
    int n;
    vector<int> room;
    int cowsT = 0;

    for(int i = 0; i < n; i++){
        cin >> room[i];
        cowsT += room[i];
    }

    //go through every room
    for(int i = 0; i < n; i++){
        int startingRoom = i;
        for(int i = )
    }  


}