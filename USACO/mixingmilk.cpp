#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
#include <set>
#include <unordered_map>
using namespace std;

int main()
{
    // freopen("mixmilk.in", "r", stdin);
    // freopen("mixmilk.out", "w", stdout);

    int in[3][2];

    for (int i = 0; i < 3; i++)
    {
        cin >> in[i][0] >> in[i][1];
    }

    
}