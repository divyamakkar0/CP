#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <numeric>
#include <set>
using namespace std;

int main()
{
    int n;
    cin >> n;

    vector<pair<char, int>> mow(n);
    for(int i = 0; i < n; i++){
        cin >> mow[i].first >> mow[i].second;
    }

    
}