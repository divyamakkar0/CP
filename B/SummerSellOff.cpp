#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
#include <unordered_map>
using namespace std;

int main()
{
    int n, f;
    cin >> n >> f;

    vector<pair<int, int>> d;
    
    for (int i = 0; i < n; i++){
        int a, b;
        cin >> a >> b;
        d.push_back(make_pair(a, b));
    }

    

}